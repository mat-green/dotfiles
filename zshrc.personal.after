# Load asdf
if [ -f $HOME/.asdf ]; then
  . $HOME/.asdf/asdf.sh
  . $HOME/.asdf/completions/asdf.bash
fi

# make sure rust is active
[ -f ~/.cargo/env ] && source $HOME/.cargo/env

alias kubectl="minikube kubectl --"

export PATH=$PATH:/usr/local/lib/go/bin

